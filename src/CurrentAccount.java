
public class CurrentAccount extends Account {

	public CurrentAccount(int nbAccount, int agencyCode, User user, float balance, boolean overdraft) {
		super(nbAccount, agencyCode, user, balance, overdraft);
	}
}
